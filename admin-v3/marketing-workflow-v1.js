(function(){
'use strict';
const C=window.DA_ADMIN_V3_CONFIG||{},AUTH_KEY='da_admin_v3_auth';let root=null,role='',data={assets:[],jobs:[],calendar:[]};
const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const fmt=v=>v?new Date(v).toLocaleString('pt-BR'):'—';
function auth(){try{return JSON.parse(localStorage.getItem(AUTH_KEY)||'null')}catch{return null}}
async function call(action,payload={}){const a=auth();if(!a?.access_token)throw new Error('Faça login.');const r=await fetch(`${C.supabaseUrl}/functions/v1/${C.marketingWorkflowEdgeFunction||'admin-marketing-workflow-v1'}`,{method:'POST',headers:{apikey:C.supabasePublishableKey,Authorization:`Bearer ${a.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({action,...payload})});const d=await r.json().catch(()=>({}));if(!r.ok||d.ok===false)throw new Error(d.detail||d.error||`Erro ${r.status}`);return d}
function notify(m){const box=document.getElementById('toastRegion');if(!box)return;const x=document.createElement('div');x.className='toast';x.textContent=m;box.appendChild(x);setTimeout(()=>x.remove(),3500)}
function addUi(){root=document.getElementById('marketingCenterMount');if(!root)return false;const tabs=root.querySelector('.marketing-tabs');if(!tabs||root.querySelector('[data-mkt-tab="calendar"]'))return !!tabs;const b=document.createElement('button');b.className='marketing-tab';b.dataset.mktTab='calendar';b.textContent='Calendário e aprovação';tabs.appendChild(b);const pane=document.createElement('section');pane.className='marketing-pane';pane.dataset.mktPane='calendar';pane.innerHTML=`<div class="marketing-grid"><section class="panel"><div class="panel-head"><div><div class="eyebrow">Revisão</div><h2>Conteúdos para aprovação</h2></div><button id="mktWfReload" class="button secondary small">Atualizar</button></div><div id="mktReviewList" class="marketing-list"></div></section><section class="panel"><div class="panel-head"><div><div class="eyebrow">Agenda</div><h2>Programar conteúdos aprovados</h2></div><span class="marketing-pill off">sem publicação</span></div><div id="mktApprovedJobs" class="marketing-list"></div></section></div><section class="panel"><div class="panel-head"><div><div class="eyebrow">Calendário editorial</div><h2>Próximos 31 dias</h2></div></div><div id="mktCalendarGrid" class="marketing-calendar"></div></section>`;root.querySelector('.marketing-center').appendChild(pane);
 b.onclick=()=>{root.querySelectorAll('[data-mkt-tab]').forEach(x=>x.classList.toggle('active',x===b));root.querySelectorAll('[data-mkt-pane]').forEach(x=>x.classList.toggle('active',x===pane));load().catch(e=>notify(e.message))};pane.querySelector('#mktWfReload').onclick=()=>load().catch(e=>notify(e.message));return true}
function render(){if(!root)return;const review=root.querySelector('#mktReviewList'),approved=root.querySelector('#mktApprovedJobs'),cal=root.querySelector('#mktCalendarGrid');if(!review)return;const reviewable=data.assets.filter(a=>['draft','rendered','review'].includes(a.status));review.innerHTML=reviewable.length?reviewable.map(a=>`<div class="marketing-item"><div class="marketing-item-head"><div><strong>${esc(a.title)}</strong><small>${esc(a.media_kind)} · ${esc(a.generation_mode)} · ${esc(a.status)}</small></div><span class="marketing-pill ${a.status==='review'?'ai':'off'}">${esc(a.status)}</span></div><div class="marketing-actions">${['draft','rendered'].includes(a.status)?`<button class="button secondary small" data-submit-review="${a.id}">Enviar para revisão</button>`:''}${a.status==='review'&&role==='owner'?`<button class="button primary small" data-approve="${a.id}">Aprovar</button>`:''}</div></div>`).join(''):'<div class="marketing-empty">Nada aguardando revisão.</div>';
 const jobs=data.jobs.filter(j=>['approved','scheduled'].includes(j.status));approved.innerHTML=jobs.length?jobs.map(j=>`<div class="marketing-item"><div class="marketing-item-head"><div><strong>${esc(j.title||j.channel)}</strong><small>${esc(j.channel)} · ${esc(j.status)}${j.scheduled_for?' · '+fmt(j.scheduled_for):''}</small></div><span class="marketing-pill ${j.status==='scheduled'?'no-ai':'off'}">${esc(j.status)}</span></div>${role==='owner'?`<div class="marketing-schedule-row">${j.status==='approved'?`<input type="datetime-local" data-when="${j.id}"><button class="button primary small" data-schedule="${j.id}">Agendar</button>`:`<button class="button secondary small" data-unschedule="${j.id}">Desagendar</button>`}</div>`:''}</div>`).join(''):'<div class="marketing-empty">Nenhum job aprovado para agendar.</div>';
 const grouped={};for(const x of data.calendar){const k=new Date(x.scheduled_for).toLocaleDateString('pt-BR');(grouped[k]||(grouped[k]=[])).push(x)}cal.innerHTML=Object.keys(grouped).length?Object.entries(grouped).map(([day,items])=>`<article class="marketing-day"><strong>${day}</strong>${items.map(x=>`<div><span>${new Date(x.scheduled_for).toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'})}</span><small>${esc(x.title)} · ${esc(x.channel)}</small></div>`).join('')}</article>`).join(''):'<div class="marketing-empty">Nenhum conteúdo agendado.</div>';
 root.querySelectorAll('[data-submit-review]').forEach(b=>b.onclick=()=>act('submit_review',{asset_id:b.dataset.submitReview},'Enviado para revisão.'));root.querySelectorAll('[data-approve]').forEach(b=>b.onclick=()=>act('approve_asset',{asset_id:b.dataset.approve,note:'Aprovado no Admin Marketing'},'Conteúdo aprovado.'));root.querySelectorAll('[data-schedule]').forEach(b=>b.onclick=()=>{const input=root.querySelector(`[data-when="${b.dataset.schedule}"]`);if(!input?.value)return notify('Escolha data e horário.');act('schedule_job',{job_id:b.dataset.schedule,scheduled_for:new Date(input.value).toISOString()},'Conteúdo agendado. Nenhuma publicação foi feita.')});root.querySelectorAll('[data-unschedule]').forEach(b=>b.onclick=()=>act('unschedule_job',{job_id:b.dataset.unschedule},'Agendamento removido.')})}
async function act(action,payload,msg){try{await call(action,payload);notify(msg);await load()}catch(e){notify(e.message)}}
async function load(){const d=await call('workflow_overview');role=d.user?.role||'';data={assets:d.assets||[],jobs:d.jobs||[],calendar:d.calendar||[]};render()}
function boot(){if(!addUi()){setTimeout(boot,250);return}load().catch(()=>{})}
boot();
})();
