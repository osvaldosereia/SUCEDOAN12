import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
test('schema persists multi product keyframes and Gemini packages',()=>{const s=read('supabase/migrations/20260917013000_creative_storyboards.sql');for(const x of ['creative_video_project_products','creative_storyboard_keyframes','creative_storyboard_gemini_packages','second_mark','continuity_lock'])assert.match(s,new RegExp(x))});
test('director is social first and emits 5 second keyframes',()=>{const s=read('supabase/functions/creative-storyboard-director/index.ts');assert.match(s,/3 conceitos/);assert.match(s,/retenção/);assert.match(s,/massinha/);assert.match(s,/i\*5/);assert.match(s,/gemini_packages/)});
test('image generator is low cost vertical and chained',()=>{const s=read('supabase/functions/creative-storyboard-image/index.ts');assert.match(s,/gpt-image-2\.5-sunburst/);assert.match(s,/quality','low/);assert.match(s,/1024x1536/);assert.match(s,/previous_image_required/)});
test('admin uses multiple products seven images and Gemini packages instead of render video',()=>{const h=read('admin/creative-studio.html'),j=read('admin/creative-studio.js');assert.match(h,/selectedProducts/);assert.match(h,/Gerar 7 imagens/);assert.match(h,/Pacotes para Gemini/);assert.match(j,/selectedProducts=\[\]/);assert.match(j,/creative-storyboard-director/);assert.match(j,/creative-storyboard-image/);assert.match(j,/copy-prompt/);assert.doesNotMatch(h,/Renderizar vídeo/)});