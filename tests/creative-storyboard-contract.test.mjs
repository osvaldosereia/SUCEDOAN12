import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
test('schema persists multi product keyframes and Gemini packages',()=>{const s=read('supabase/migrations/20260917013000_creative_storyboards.sql');for(const x of ['creative_video_project_products','creative_storyboard_keyframes','creative_storyboard_gemini_packages','second_mark','continuity_lock'])assert.match(s,new RegExp(x))});
test('director is social first and emits 5 second keyframes plus visual bible',()=>{const s=read('supabase/functions/creative-storyboard-director/index.ts');assert.match(s,/3 conceitos/);assert.match(s,/retenção/);assert.match(s,/massinha/);assert.match(s,/i\*5/);assert.match(s,/gemini_packages/);assert.match(s,/visual_bible/);assert.match(s,/lock_visual_identity/)});
test('image generator is low cost vertical, saves candidate and resolves previous approval server-side',()=>{const s=read('supabase/functions/creative-storyboard-image/index.ts');assert.match(s,/gpt-image-2\.5-sunburst/);assert.match(s,/quality','low/);assert.match(s,/1024x1536/);assert.match(s,/candidate_image_url:publicUrl/);assert.match(s,/approved_image_url:null/);assert.match(s,/second-5/);assert.match(s,/prev\.data\?\.approved_image_url/);assert.match(s,/visual_identity_required/)});
test('projects backend guards approval and can invalidate or reopen downstream frames',()=>{const s=read('supabase/functions/creative-storyboard-projects/index.ts');assert.match(s,/previous_frame_not_approved/);assert.match(s,/candidate_required/);assert.match(s,/visual_identity_required/);assert.match(s,/invalidate_after/);assert.match(s,/reopen_frame/);assert.match(s,/status:'stale'/)});
test('admin approves visual identity and only chains approved images',()=>{const h=read('admin/creative-studio.html'),j=read('admin/creative-studio.js');assert.match(h,/selectedProducts/);assert.match(h,/Aprovar identidade visual/);assert.match(h,/Pacotes para Gemini/);assert.match(j,/approved_image_url/);assert.match(j,/candidate_image_url/);assert.match(j,/visualBible/);assert.match(j,/refs\.every\(f=>f\?\.approved_image_url/);assert.doesNotMatch(h,/Gerar 7 imagens/);assert.doesNotMatch(h,/Renderizar vídeo/)});