import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
test('Admin publica Estúdio Criativo',()=>{const h=read('admin/index.html');assert.match(h,/creative-studio\.html/);assert.match(h,/Estúdio Criativo/)});
test('briefing oferece produção completa rápida institucional e mosaico 6x6',()=>{const h=read('admin/creative-studio.html');for(const s of ['productionMode','referenceUpload','institutionalGoal'])assert.match(h,new RegExp(`id="${s}"`));for(const v of ['full','product_only','institutional','mosaic_6x6'])assert.match(h,new RegExp(`value="${v}"`))});
test('todos os segmentos Gemini são de 10 segundos',()=>{const d=read('supabase/functions/creative-storyboard-director/index.ts');assert.match(d,/length:d\/10/);assert.match(d,/end_second:i\*10\+10/)});
test('modo rápido não exige geração de keyframes visuais',()=>{const j=read('admin/creative-studio.js');assert.match(j,/production_mode/);assert.match(j,/product_only/);assert.match(j,/skip_visual_generation/)});
test('modo institucional permite criar sem produto do banco',()=>{const j=read('admin/creative-studio.js'),d=read('supabase/functions/creative-storyboard-director/index.ts');assert.match(j,/institutional/);assert.match(j,/reference_image/);assert.match(d,/mode!==\'institutional\'&&!products\.length/)});
test('produto e imagens geradas oferecem download',()=>{const j=read('admin/creative-studio.js'),u=read('admin/creative-studio-ui-helpers.js');assert.match(j,/downloadAsset/);assert.match(j,/Baixar foto original/);assert.match(j,/Baixar imagem/);assert.match(u,/Baixar foto original/);assert.match(u,/Baixar frame/)});
test('pacotes Gemini mobile exibem produto e copiar prompt com fallback',()=>{const u=read('admin/creative-studio-ui-helpers.js'),c=read('admin/creative-studio.css'),h=read('admin/creative-studio.html');assert.match(u,/package-product-ref/);assert.match(u,/Copiar prompt Gemini/);assert.match(u,/execCommand\('copy'\)/);assert.match(c,/@media\(max-width:480px\)/);assert.match(c,/overflow-x:hidden/);assert.match(c,/gemini-package>button/);assert.match(h,/creative-studio\.css\?v=20260917-5/)});
test('pacote Veo trava produto e referências',()=>{const d=read('supabase/functions/creative-storyboard-director/index.ts');for(const s of ['REFERENCE PRIORITY','PRODUCT LOCK','STYLE LOCK','SHOT TIMELINE','CAMERA DIRECTION','AUDIO BLUEPRINT','NEGATIVE CONSTRAINTS','FINAL FRAME REQUIREMENT'])assert.match(d,new RegExp(s));assert.match(d,/foto original/i);assert.match(d,/embalagem/i);assert.match(d,/logotipo/i)});
test('plano persiste briefing completo para retomada',()=>{const j=read('admin/creative-studio.js');assert.match(j,/Object\.assign\(plan,b\)/);assert.match(j,/production_mode/);assert.match(j,/institutional_goal/)});
test('busca de produtos usa backend protegido do Estúdio',()=>{const j=read('admin/creative-studio.js'),p=read('supabase/functions/creative-storyboard-projects/index.ts');assert.match(j,/action:'search_products'/);assert.doesNotMatch(j,/adminApi\(/);assert.match(p,/action==='search_products'/)});
test('produção completa gera um frame por vez',()=>{const j=read('admin/creative-studio.js');assert.match(j,/if\(i>0&&!prev\?\.approved_image_url\)/);assert.match(j,/candidate_image_url/);assert.match(j,/approved_image_url/)});
test('primeiro frame trava identidade visual',()=>{const h=read('admin/creative-studio.html'),j=read('admin/creative-studio.js');assert.match(h,/Aprovar identidade visual/);assert.match(j,/visualBible\?\.locked/)});
test('pacotes completos exigem referências aprovadas',()=>{const j=read('admin/creative-studio.js');assert.match(j,/f\?\.status==='approved'&&f\?\.approved_image_url/);assert.match(j,/navigator\.clipboard\.writeText/)});
test('Estúdio renova JWT expirado',()=>{const j=read('admin/creative-studio.js');assert.match(j,/grant_type=refresh_token/);assert.match(j,/r\.status===401&&retry/)});
test('mosaico 6x6 usa 36 células quadradas em low e vídeo silencioso',()=>{const h=read('admin/creative-studio.html'),d=read('supabase/functions/creative-storyboard-director/index.ts'),i=read('supabase/functions/creative-storyboard-image/index.ts'),m=read('admin/creative-studio-mosaic.js');assert.match(h,/value="mosaic_6x6"/);assert.match(d,/mosaic_6x6/);assert.match(d,/36/);assert.match(d,/row-major/);assert.match(i,/mosaic_6x6/);assert.match(i,/1024x1024/);assert.match(i,/quality.*low/);assert.match(m,/GRID_SIZE\s*=\s*6/);assert.match(m,/36/);assert.match(m,/MediaRecorder/);assert.match(m,/audioBitsPerSecond:\s*0|removeTrack|sem áudio/i)});
test('projetos ficam salvos, podem ser reabertos e excluídos com confirmação',()=>{const h=read('admin/creative-studio.html'),p=read('supabase/functions/creative-storyboard-projects/index.ts'),x=read('admin/creative-studio-persistence.js');assert.match(h,/Projetos salvos/);assert.match(p,/action==='delete'/);assert.match(p,/creative_storyboard_generation_events/);assert.match(p,/creative_storyboard_gemini_packages/);assert.match(p,/creative_storyboard_keyframes/);assert.match(p,/creative_video_project_products/);assert.match(p,/creative_video_projects/);assert.match(x,/Excluir projeto/);assert.match(x,/confirm\(/);assert.match(x,/action:'delete'/);assert.match(x,/openProject|loadProject/)});