export function evaluateCost(plan={},config={}){const director=Number(plan.costs?.director??0),image=Number(plan.costs?.image_generation??0),video=Number(plan.costs?.video_generation??0),other=Number(plan.costs?.other_paid??0),estimated=director+image+video+other,limit=Number(config.maxPaidCostPerVideo??.20),paid=image+video+other,blocked=paid>0&&estimated>limit;return{estimated:Number(estimated.toFixed(6)),requiresApproval:blocked,allowedActions:{procedural:true,local_asset:true,free_licensed_asset:true,director:true,paid_generation:!blocked},limit}}
