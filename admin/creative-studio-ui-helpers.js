function renderVisualBible(){const el=document.getElementById('visualBibleStatus');if(!el)return;const locked=!!visualBible?.locked;el.textContent=locked?'Identidade visual mestre aprovada. Frames seguintes usam a Bíblia Visual + imagem anterior aprovada.':'A identidade visual será travada quando o primeiro frame for aprovado.';el.className=`studio-search-status ${locked?'approved':''}`;}