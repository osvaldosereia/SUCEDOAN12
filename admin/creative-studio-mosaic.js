(()=>{'use strict';
const GRID_SIZE=6,FRAME_COUNT=36;
async function loadImage(src){return await new Promise((resolve,reject)=>{const img=new Image();img.crossOrigin='anonymous';img.onload=()=>resolve(img);img.onerror=reject;img.src=src})}
async function splitMosaic(src){const img=await loadImage(src),side=Math.min(img.naturalWidth,img.naturalHeight),cell=side/GRID_SIZE,frames=[];for(let row=0;row<GRID_SIZE;row++)for(let col=0;col<GRID_SIZE;col++){const canvas=document.createElement('canvas');canvas.width=canvas.height=Math.floor(cell);const ctx=canvas.getContext('2d');ctx.drawImage(img,col*cell,row*cell,cell,cell,0,0,canvas.width,canvas.height);frames.push(canvas)}return frames}
async function buildSilentVideo(src,durationSeconds=10){const frames=await splitMosaic(src);if(!window.MediaRecorder)throw new Error('video_not_supported');const canvas=document.createElement('canvas');canvas.width=canvas.height=720;const ctx=canvas.getContext('2d'),stream=canvas.captureStream(30);stream.getAudioTracks().forEach(track=>stream.removeTrack(track));const chunks=[],mime=['video/webm;codecs=vp9','video/webm;codecs=vp8','video/webm'].find(x=>MediaRecorder.isTypeSupported(x))||'';const recorder=new MediaRecorder(stream,mime?{mimeType:mime}:undefined);recorder.ondataavailable=e=>{if(e.data?.size)chunks.push(e.data)};const done=new Promise((resolve,reject)=>{recorder.onstop=()=>resolve(new Blob(chunks,{type:mime||'video/webm'}));recorder.onerror=reject});recorder.start();const frameMs=durationSeconds*1000/FRAME_COUNT;for(const frame of frames){ctx.clearRect(0,0,canvas.width,canvas.height);ctx.drawImage(frame,0,0,canvas.width,canvas.height);await new Promise(r=>setTimeout(r,frameMs))}recorder.stop();return await done}
window.CreativeStudioMosaic={GRID_SIZE,FRAME_COUNT,splitMosaic,buildSilentVideo};
})();