name: Build WhatsApp Flow Images — arquivado
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Imagens do Flow antigo arquivadas; sem automação automática."
