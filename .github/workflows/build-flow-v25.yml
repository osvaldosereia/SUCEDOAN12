name: Build WhatsApp Flow V25 — arquivado
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "WhatsApp Flow próprio arquivado após migração para PapoAI."
