name: CanecaFácil · testar ciclo produto temporário LI

on:
  push:
    branches: [main]
    paths:
      - 'scripts/testar-ciclo-produto-temporario-li.mjs'
      - '.github/workflows/testar-ciclo-produto-temporario-li.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  testar:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            scripts/testar-ciclo-produto-temporario-li.mjs
          sparse-checkout-cone-mode: false
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Carregar Personal Token
        env:
          LI_APP_TOKEN: ${{ secrets.LOJA_INTEGRADA_APP_KEY }}
        run: |
          test -n "$LI_APP_TOKEN" || { echo 'Token ausente'; exit 1; }
          printf 'LOJA_INTEGRADA_AUTHORIZATION=%s\n' "$LI_APP_TOKEN" >> "$GITHUB_ENV"
      - name: Ativar brevemente e desativar ao final
        env:
          LOJA_INTEGRADA_BASE_URL: https://api.awsli.com.br/v1
        run: node scripts/testar-ciclo-produto-temporario-li.mjs
