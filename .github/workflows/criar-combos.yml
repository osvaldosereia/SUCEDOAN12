name: Criar combos automaticos

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"
      timezone: "America/Cuiaba"

permissions:
  contents: write

jobs:
  criar-combos:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar repositorio
        uses: actions/checkout@v4

      - name: Configurar Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Criar combos inteligentes
        run: node scripts/criar-combos.js
        env:
          FIREBASE_URL: ${{ secrets.FIREBASE_URL }}

      - name: Salvar site/combo.json no GitHub
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"

          echo "Arquivos alterados:"
          git status --short

          if [ -f "site/combo.json" ]; then
            git add site/combo.json
          else
            echo "ERRO: site/combo.json não foi encontrado."
            echo "Arquivos existentes na pasta site:"
            ls -la site || true
            exit 1
          fi

          if git diff --cached --quiet; then
            echo "Nenhuma alteração para salvar."
          else
            git commit -m "Atualiza combos automaticos"
            git push
          fi
