name: Patch Agent Core Semantic Boundaries — arquivado
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Patch legado do agente próprio arquivado."
