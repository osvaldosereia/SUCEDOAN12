name: Dona Antonia Stage 7 Messenger — arquivado

# Runtime Messenger próprio não faz parte da arquitetura atual PapoAI + vitrine.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Messenger runtime próprio arquivado; sem gatilhos automáticos."
