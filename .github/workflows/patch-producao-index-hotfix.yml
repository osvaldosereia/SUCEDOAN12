name: Hotfix admin banners e Firebase

on:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/patch-producao-index-hotfix.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Aplicar hotfix em producao/index.html e banners JSON
        run: