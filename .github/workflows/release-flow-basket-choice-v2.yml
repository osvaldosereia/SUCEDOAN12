name: Release Flow Basket Choice V2 — arquivado
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release do Flow próprio arquivado."
