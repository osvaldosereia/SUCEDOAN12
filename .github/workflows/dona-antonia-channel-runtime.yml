name: Dona Antonia channel runtime — arquivado

# Adaptadores próprios de atendimento ficaram fora da arquitetura atual PapoAI + vitrine.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Channel runtime próprio arquivado; sem gatilhos automáticos."
