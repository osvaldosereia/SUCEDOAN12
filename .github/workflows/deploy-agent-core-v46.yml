name: Deploy Agent Core — arquivado
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy do agente próprio desativado após migração para PapoAI."
