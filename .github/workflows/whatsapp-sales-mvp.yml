name: Dona Antonia WhatsApp Sales MVP — arquivado
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "MVP de vendas WhatsApp próprio arquivado; PapoAI é o canal ativo."
